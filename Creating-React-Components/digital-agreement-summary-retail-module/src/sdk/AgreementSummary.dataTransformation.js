class AgreementsummaryTransformation {
    /**
     * ToDo: Remove after actual implementation
     * Added as a blue print
     */
    constructor() {
        this.sayHello = this.sayHello.bind(this);
    }

    sayHello () {
        const hello = 'Hello Data Transformation';
        return hello;
    }

}

export default AgreementsummaryTransformation;
