import Form, {INITIALIZED, SUBMITTED} from './form';
import messages from './form.i18n';

const components = {
    Form,
    INITIALIZED,
    SUBMITTED,
    messages
};

export default components;
